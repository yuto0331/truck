<h1>Sessions#newあとで変更</h1>
<h4>セッションメソッドなので、閉じると破棄される。  Rails提供のメソッド</h4>
<%= form_with(scope: :session, url: sessions_path) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>
  
  <%= f.label :password %>
  <%= f.password_field :password %>
  
  <%= f.submit "Log in" %>
<% end %>